// const emailInstance = fetch.create({
//     baseURL: 'https://flipkart-email-mock.now.sh/',
// });

// emailInstance.interceptors.request.use((config) => {
//     config.params = {
//         ...config.params,
//     }
//     return config
// })

// export default emailInstance;